[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
	# deploy_dir = deploy
	# revert_dir = revert
	# verify_dir = verify
	# extension = sql
[core "pg"]
	target = db:pg://facile@localhost/facile
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
[rebase]
	verify = true
